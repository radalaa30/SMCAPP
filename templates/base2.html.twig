<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Gestion Cariste{% endblock %}</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            padding-top: 56px; /* Hauteur de la navbar */
        }
        .navbar {
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
        }
        .main-content {
            margin-top: 20px;
            margin-bottom: 20px;
            padding: 0 15px;
        }
        .table-container {
            height: calc(100vh - 120px);
            overflow-y: auto;
        }
        .table thead th {
            position: sticky;
            top: 0;
            background-color: #343a40;
            z-index: 1;
        }
        .dropdown-menu {
            border-radius: 0;
            border: none;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
        }
        .dropdown-item:hover {
            background-color: #0d6efd;
            color: white;
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Application SMC</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_dashboard') }}">Liste Réappro</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_suivi_stats') }}">Statistiques</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_import_suivi_preparation') }}">Mise à Jours</a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="gdsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            GDS
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="gdsDropdown">
                            <li><a class="dropdown-item" href="{{ path('app_dashboard_inventaire') }}">Stock Live</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_suivi_preparation_search') }}">Historique des préparations</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('app_import_inventaire') }}">Mise à Jours GDS</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="main-content">
        {% include 'partials/flash.html.twig' %}
        {% block body %}{% endblock %}
    </main>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/js/all.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>