<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Tableau de bord — SMC{% endblock %}</title>

    {# CDN #}
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">

    <style>
        :root{
            --brand:#0d6efd;
            --brand-2:#6c63ff;
            --ink:#212529;
            --muted:#6c757d;
            --card-bg:#ffffff;
            --body-bg:#0b1620;        /* fallback si l'image ne charge pas */
            --border:#e9ecef;
            --success:#20c997;
            --warning:#ffc107;
            --danger:#ef5350;
            --info:#17a2b8;

            /* overlay au-dessus de l'image pour le contraste */
            --bg-overlay: rgba(8, 15, 25, .55);
        }

        html,body { height: 100%; }

        /* ==== IMAGE D'ARRIÈRE-PLAN ==== */
        body.bg-image{
            background:
                linear-gradient(var(--bg-overlay), var(--bg-overlay)),
                url('{{ asset("img/dep.png") }}') center / cover no-repeat fixed,
                var(--body-bg);
            color: var(--ink);
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            padding-top: 62px; /* espace sous navbar fixe */
        }

        /* Perf mobile : éviter background-attachment: fixed */
        @media (max-width: 991.98px){
            body.bg-image{
                background-attachment: scroll;
                background-position: center top;
            }
        }

        /* OPTION : dégradé sans image (si tu veux basculer rapidement) */
        body.bg-gradient{
            padding-top: 62px;
            background:
                radial-gradient(1200px 800px at 80% 0%, #2cc0d3 0%, transparent 50%),
                radial-gradient(900px 700px at 0% 100%, #1976d2 0%, transparent 45%),
                linear-gradient(180deg, #0e2433, #0b1a26 60%, #0a1722 100%);
            color: var(--ink);
        }

        /* Navbar */
        .navbar {
            position: fixed; top:0; left:0; right:0; z-index:1030;
            box-shadow: 0 4px 18px rgba(0,0,0,.18);
            background: linear-gradient(90deg, var(--brand) 0%, var(--brand-2) 100%);
        }
        .navbar .nav-link { font-weight: 500; letter-spacing:.2px; }
        .navbar .dropdown-menu { border:0; box-shadow:0 8px 24px rgba(0,0,0,.12); }
        .dropdown-item:hover { background: var(--brand); color:#fff; }

        /* Sous-menu au survol (desktop) */
        .dropdown-submenu { position: relative; }
        .dropdown-submenu > .dropdown-menu {
            top: 0; left: 100%;
            margin-left: .25rem; margin-right: .25rem;
        }
        @media (min-width: 992px) {
            .dropdown-submenu:hover > .dropdown-menu { display: block; }
        }

        /* Layout de base */
        .main-content { padding: 24px; }

        /* ===== Réduction de la largeur du <main> pour voir l'arrière-plan ===== */
        .main-content.main-narrow{
            /* largeur max responsive et centrée */
            max-width: clamp(960px, 96vw, 1280px);
            width: 100%;
            margin: 20px auto 40px;    /* marges haut/bas et centrage */
            padding-left: 16px;         /* un peu d'air sur les côtés */
            padding-right: 16px;
        }
        @media (min-width: 1600px){
            .main-content.main-narrow{ max-width: 1320px; }
        }

        /* Cartes, tableaux & helpers (inchangés) */
        .card {
            background: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: 14px;
            box-shadow: 0 4px 14px rgba(0,0,0,.07);
        }
        .card-header {
            border-bottom: 1px solid var(--border);
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #fff;
            border-radius: 14px 14px 0 0;
        }
        .table thead th {
            position: sticky; top: 0; z-index: 5;
            background: #f8f9fc; border-bottom: 1px solid var(--border);
        }
        .table td, .table th { vertical-align: middle; }
        .table .num { text-align: right; font-variant-numeric: tabular-nums; }
        .badge-soft { background: #eef1ff; color: var(--brand); border:1px solid #e0e4ff; }
        .kpi { display:flex; align-items:center; gap:.75rem; }
        .kpi .icon{
            width:42px;height:42px;border-radius:10px;
            display:grid; place-items:center;
            background:#eef4ff; color:var(--brand);
        }
        .chart-wrap { height: 420px; }
        .section-title { margin:0; font-weight:700; letter-spacing:.2px; }
        .section-sub { color:#f0f0f0; opacity:.9; }
        .page-end { height: 12px; }
    </style>

    {% block stylesheets %}{% endblock %}
</head>

{# Applique l'image de fond ici #}
<body class="bg-image">
<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid px-3">
        <a class="navbar-brand fw-bold" href="#">SMC Dashboard</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#topnav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div id="topnav" class="collapse navbar-collapse">
            <ul class="navbar-nav me-auto">

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_dashboard') }}">
                        <i class="fa-solid fa-list-check me-1"></i> Liste Réappro
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_suivi_stats') }}">
                        <i class="fa-solid fa-chart-line me-1"></i> Statistiques
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_import_suivi_preparation') }}">
                        <i class="fa-solid fa-rotate me-1"></i> Mise à jour
                    </a>
                </li>

                {# Dropdown GDS #}
                <li class="nav-item dropdown" data-bs-auto-close="outside">
                    <a class="nav-link dropdown-toggle" href="#" id="gdsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fa-solid fa-box-archive me-1"></i> GDS
                    </a>

                    <ul class="dropdown-menu" aria-labelledby="gdsDropdown">

                        <li>
                            <a class="dropdown-item" href="{{ path('app_dashboard_inventaire') }}">
                                <i class="fa-solid fa-warehouse me-2"></i> Stock Live
                            </a>
                        </li>

                        {# Sous-menu Produit #}
                        <li class="dropdown-submenu dropend">
                            <a class="dropdown-item dropdown-toggle" href="#" id="submenuProduit" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fa-solid fa-boxes-stacked me-2"></i> Produit
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="submenuProduit">
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_liste_produits_index') }}">
                                        <i class="fa-regular fa-rectangle-list me-2"></i> Parcourir / Rechercher la liste
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_recherche_code_produit') }}">
                                        <i class="fa-solid fa-magnifying-glass me-2"></i> Recherche produit en masse
                                    </a>
                                </li>
                            </ul>
                        </li>
                        {# /Sous-menu Produit #}

                        <li>
                            <a class="dropdown-item" href="{{ path('app_suivi_preparation_search') }}">
                                <i class="fa-regular fa-clock me-2"></i> Historique des préparations
                            </a>
                        </li>

                        <li><hr class="dropdown-divider"></li>

                        <li>
                            <a class="dropdown-item" href="{{ path('app_emplacements') }}">
                                <i class="fa-solid fa-location-dot me-2"></i> Plein / Vide
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="{{ path('app_import_inventaire') }}">
                                <i class="fa-solid fa-file-arrow-up me-2"></i> Mise à jour GDS (Inventaire)
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="{{ path('app_import_produits') }}">
                                <i class="fa-solid fa-file-arrow-up me-2"></i> Mise à jour GDS (Produits)
                            </a>
                        </li>
                          <li>
                            <a class="dropdown-item" href="{{ path('admin_users_index') }}">
                                <i class="fa-solid fa-file-arrow-up me-2"></i> Utilisateurs
                            </a>
                        </li>

                    </ul>
                </li>
                {# /Dropdown GDS #}
            </ul>

            {# Right side #}
            <div class="d-flex align-items-center gap-2">
                <span class="badge badge-soft">v1.0</span>
            </div>
        </div>
    </div>
</nav>

{# Ajout de la classe main-narrow pour limiter la largeur #}
<main class="main-content container-fluid main-narrow">
    {% include 'partials/flash.html.twig' %}
    {% block body %}{% endblock %}
</main>

<div class="page-end"></div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/js/all.min.js"></script>
{% block javascripts %}{% endblock %}
</body>
</html>
