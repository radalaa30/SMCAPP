{% extends 'base2.html.twig' %}

{% block title %}Accès refusé{% endblock %}

{% block body %}
  <div class="container py-5">
    <h1 class="mb-3">Accès refusé</h1>
    <p>Vous n’avez pas l’autorisation de consulter cette page.</p>
    {% if app.user %}
      <p class="text-muted">Connecté en tant que <strong>{{ app.user.userIdentifier }}</strong>.</p>
    {% endif %}
    <a class="btn btn-primary" href="{{ path('app_redirect') }}">Retour</a>
  </div>
{% endblock %}